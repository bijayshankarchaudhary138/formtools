(function(){
const $=s=>document.querySelector(s);
function dl(blob,name){const u=URL.createObjectURL(blob),a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),2500)}
function status(t,ok=false){const e=$('#status');if(e){e.textContent=t;e.className='status '+(ok?'ok':'')}}
function fileInput(){return $('#files')||$('#fileInput')}
async function loadScript(src,global){if(window[global])return window[global];await new Promise((res,rej)=>{const s=document.createElement('script');s.src=src;s.async=true;s.onload=res;s.onerror=rej;document.head.appendChild(s)});return window[global]}
async function pdfjs(){const lib=await loadScript('https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.4.168/pdf.min.js','pdfjsLib');lib.GlobalWorkerOptions.workerSrc='https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.4.168/pdf.worker.min.js';return lib}
async function jszip(){return loadScript('https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js','JSZip')}
function setup(){const input=fileInput(),drop=$('#dropzone')||$('#drop');if(!input)return;if(drop){drop.onclick=()=>input.click();drop.ondragover=e=>e.preventDefault();drop.ondrop=e=>{e.preventDefault();if(e.dataTransfer?.files?.length){try{input.files=e.dataTransfer.files}catch{}input.dispatchEvent(new Event('change'))}}}input.addEventListener('change',()=>{const l=$('#list');if(l)l.textContent=[...input.files].map((f,i)=>`${i+1}. ${f.name}`).join(' • ')||'No files selected.'})}
async function pdfMerge(){try{const f=[...(fileInput()?.files||[])];if(f.length<2)return status('Select at least 2 PDF files.');status('Merging…');const out=await PDFLib.PDFDocument.create();for(const x of f){const d=await PDFLib.PDFDocument.load(await x.arrayBuffer());(await out.copyPages(d,d.getPageIndices())).forEach(p=>out.addPage(p))}dl(new Blob([await out.save()],{type:'application/pdf'}),'formtools-merged.pdf');status(`Done • ${out.getPageCount()} pages`,true)}catch(e){console.error(e);status('Could not merge this PDF. Try another file.')}}
function parsePages(v,max){const a=new Set();for(const part of v.split(',')){const [x,y]=part.trim().split('-').map(Number);if(!x)continue;if(y){for(let n=Math.min(x,y);n<=Math.max(x,y);n++)if(n>=1&&n<=max)a.add(n)}else if(x>=1&&x<=max)a.add(x)}return [...a].sort((a,b)=>a-b)}
async function pdfSplit(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select a PDF.');const d=await PDFLib.PDFDocument.load(await f.arrayBuffer()),pages=parsePages($('#pages')?.value||'1',d.getPageCount());if(!pages.length)return status('Enter page numbers like 1,3-5.');status('Creating split ZIP…');const zip=await jszip();for(const n of pages){const out=await PDFLib.PDFDocument.create();const [p]=await out.copyPages(d,[n-1]);out.addPage(p);zip.file(`page-${n}.pdf`,await out.save())}dl(await zip.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:6}}),'formtools-split-pdf.zip');status(`Done • ${pages.length} PDF(s) in one ZIP`,true)}catch(e){console.error(e);status('Could not split this PDF. Check the page numbers.')}}
async function pdfRotate(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select a PDF.');const d=await PDFLib.PDFDocument.load(await f.arrayBuffer()),deg=Number($('#degrees')?.value||$('#deg')?.value||90);d.getPages().forEach(p=>p.setRotation(PDFLib.degrees((p.getRotation().angle+deg)%360)));dl(new Blob([await d.save()],{type:'application/pdf'}),'rotated.pdf');status('Rotated PDF downloaded.',true)}catch(e){console.error(e);status('Could not rotate this PDF.')}}
async function imgToPdf(){try{const fs=[...(fileInput()?.files||[])];if(!fs.length)return status('Select image(s).');const out=await PDFLib.PDFDocument.create();for(const f of fs){const b=await f.arrayBuffer();let im;if(f.type==='image/png')im=await out.embedPng(b);else if(f.type==='image/jpeg')im=await out.embedJpg(b);else{const img=new Image(),u=URL.createObjectURL(f);await new Promise((r,j)=>{img.onload=r;img.onerror=j;img.src=u});const c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;c.getContext('2d').drawImage(img,0,0);URL.revokeObjectURL(u);im=await out.embedJpg(await new Promise(r=>c.toBlob(r,'image/jpeg',.94)))}const maxW=595,maxH=842,scale=Math.min(maxW/im.width,maxH/im.height,1),page=out.addPage([im.width*scale,im.height*scale]);page.drawImage(im,{x:0,y:0,width:im.width*scale,height:im.height*scale})}dl(new Blob([await out.save()],{type:'application/pdf'}),'images.pdf');status(`PDF created • ${fs.length} image(s)`,true)}catch(e){console.error(e);status('Could not create the PDF from these images.')}}
async function pdfToJpg(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select a PDF.');status('Rendering pages…');const lib=await pdfjs(),doc=await lib.getDocument({data:await f.arrayBuffer()}).promise,zip=await jszip();for(let i=1;i<=doc.numPages;i++){const p=await doc.getPage(i),v=p.getViewport({scale:1.8}),c=document.createElement('canvas');c.width=Math.ceil(v.width);c.height=Math.ceil(v.height);await p.render({canvasContext:c.getContext('2d'),viewport:v}).promise;const b=await new Promise(r=>c.toBlob(r,'image/jpeg',.9));zip.file(`page-${i}.jpg`,await b.arrayBuffer());status(`Rendering page ${i} of ${doc.numPages}…`)}dl(await zip.generateAsync({type:'blob',compression:'DEFLATE',compressionOptions:{level:3}}),'formtools-pdf-to-jpg.zip');status(`${doc.numPages} JPG page(s) packed into one ZIP.`,true)}catch(e){console.error(e);status('Could not convert this PDF. Try a smaller file.')}}
async function pdfText(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select a PDF.');const lib=await pdfjs(),doc=await lib.getDocument({data:await f.arrayBuffer()}).promise;let text='';for(let i=1;i<=doc.numPages;i++){const p=await doc.getPage(i),c=await p.getTextContent();text+=`\n\n--- Page ${i} ---\n`+c.items.map(x=>x.str).join(' ')}dl(new Blob([text.trim()],{type:'text/plain;charset=utf-8'}),'pdf-text.txt');status('Text extracted.',true)}catch(e){console.error(e);status('Could not extract text. Scanned PDFs need OCR.')}}
async function imageOCR(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select an image.');status('Loading OCR engine/model… first use can take time.');await loadScript('https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js','Tesseract');const lang=$('#language')?.value||'eng';const r=await Tesseract.recognize(f,lang,{logger:m=>{if(m.status&&m.progress)status(`${m.status} ${Math.round(m.progress*100)}%`)}});if($('#output'))$('#output').value=r.data.text;status('OCR complete.',true)}catch(e){console.error(e);status('OCR failed. Try a clearer/smaller image.')}}
async function pdfCompress(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select a PDF.');const d=await PDFLib.PDFDocument.load(await f.arrayBuffer(),{updateMetadata:false});d.setTitle('');d.setAuthor('');d.setSubject('');d.setKeywords([]);d.setProducer('FormTools');const b=await d.save({useObjectStreams:true,addDefaultPage:false});dl(new Blob([b],{type:'application/pdf'}),'compressed.pdf');const ratio=Math.round((1-b.length/f.size)*100);status(`Optimized • ${Math.max(0,ratio)}% size reduction. Image-heavy PDFs may need raster compression.`,true)}catch(e){console.error(e);status('Could not optimize this PDF.')}}
async function editor(){try{const f=fileInput()?.files?.[0];if(!f)return status('Select a PDF.');const d=await PDFLib.PDFDocument.load(await f.arrayBuffer());const text=$('#editText')?.value||'FormTools';const pages=d.getPages(),pageIndex=Math.max(0,Math.min(pages.length-1,(Number($('#page')?.value||1)-1)));const page=pages[pageIndex];page.drawText(text,{x:Number($('#x')?.value||50),y:Number($('#y')?.value||50),size:Number($('#size')?.value||18),maxWidth:page.getWidth()-100});dl(new Blob([await d.save()],{type:'application/pdf'}),'edited.pdf');status(`Text added to page ${pageIndex+1}.`,true)}catch(e){console.error(e);status('Could not edit this PDF.')}}
function qr(){const t=$('#qrText')?.value||'';if(!t)return status('Enter text or URL.');const c=$('#canvas');QRCode.toCanvas(c,t,{width:800,margin:2},e=>{if(e)return status('QR error');c.toBlob(b=>dl(b,'qr-code.png'),'image/png');status('QR generated.',true)})}
function barcode(){const t=$('#barcodeText')?.value||'';if(!t)return status('Enter barcode data.');const c=$('#canvas');bwipjs.toCanvas(c,{bcid:'code128',text:t,scale:3,height:12,includetext:true},e=>{if(e)return status('Barcode error');c.toBlob(b=>dl(b,'barcode.png'),'image/png');status('Barcode generated.',true)})}
async function rename(){try{const fs=[...(fileInput()?.files||[])];if(!fs.length)return status('Select files.');const zip=await jszip();fs.forEach((f,i)=>{const dot=f.name.lastIndexOf('.'),ext=dot>=0?f.name.slice(dot):'';zip.file(`${$('#prefix')?.value||'file'}-${String(i+1).padStart(2,'0')}${ext}`,f)});dl(await zip.generateAsync({type:'blob',compression:'DEFLATE'}),'renamed-files.zip');status('ZIP created.',true)}catch(e){console.error(e);status('Could not create ZIP.')}}
async function photoSheet(){try{const fs=[...(fileInput()?.files||[])].filter(f=>f.type.startsWith('image/'));if(!fs.length)return status('Select one or more photos.');const imgs=await Promise.all(fs.map(f=>new Promise((r,j)=>{const im=new Image();const u=URL.createObjectURL(f);im.onload=()=>{URL.revokeObjectURL(u);r(im)};im.onerror=j;im.src=u})));const cols=4,cellW=300,cellH=400,rows=Math.ceil(imgs.length/cols),c=$('#canvas'),ctx=c.getContext('2d');c.width=cols*cellW;c.height=rows*cellH;ctx.fillStyle='#fff';ctx.fillRect(0,0,c.width,c.height);imgs.forEach((img,i)=>{const x=(i%cols)*cellW,y=Math.floor(i/cols)*cellH,scale=Math.min((cellW-40)/img.naturalWidth,(cellH-40)/img.naturalHeight);const w=img.naturalWidth*scale,h=img.naturalHeight*scale;ctx.drawImage(img,x+(cellW-w)/2,y+(cellH-h)/2,w,h)});const b=await new Promise(r=>c.toBlob(r,'image/jpeg',.95));dl(b,'photo-sheet.jpg');status(`${imgs.length} photo(s) placed on the sheet.`,true)}catch(e){console.error(e);status('Could not create the photo sheet.')}}
function scan(){const f=fileInput()?.files?.[0];if(!f)return status('Select an image.');const img=new Image(),u=URL.createObjectURL(f);img.onload=()=>{URL.revokeObjectURL(u);const c=$('#canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;const x=c.getContext('2d');x.filter='grayscale(1) contrast(1.35) brightness(1.08)';x.drawImage(img,0,0);c.toBlob(b=>dl(b,'clean-scan.jpg'),'image/jpeg',.9);status('Clean scan created.',true)};img.src=u}
function typing(){const i=$('#textInput'),o=$('#stats');if(!i||!o)return;const u=()=>{const t=i.value;o.textContent=`${t.length} characters • ${t.trim()?t.trim().split(/\s+/).length:0} words • ${t.split(/\n/).length} lines`};i.oninput=u;u()}
window.FT={setup,pdfMerge,pdfSplit,pdfRotate,imgToPdf,pdfToJpg,pdfText,imageOCR,pdfCompress,editor,qr,barcode,rename,photoSheet,scan,typing,status};document.addEventListener('DOMContentLoaded',setup);
})();
